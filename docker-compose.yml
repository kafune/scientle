services:
  app:
    build: .
    image: spotle-cientifico:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Só escuta no localhost da VPS; o nginx do host faz o proxy + SSL.
    ports:
      - "127.0.0.1:3000:3000"
